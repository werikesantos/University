SELECT 
	a.* #SELECIONANDO TODOS OS ATRIBUTOS DA TABELA ALUNO
FROM
	avaliacao av, aluno a #CHAMANDO DUAS TABELAS PARA ASSOCIAÇÃO
WHERE
	av.numero_prova = 1 #ATRIBUINDO UMA CONDIÇÃO DE UM ATRIBUTO DA TABELA AVALIAÇÃO
	AND a.codigo = av.codigo_aluno; #LIGANDO A CHAVE ESTRANGEIRA, PARA QUE NÃO TENHA DUPLICAÇÃO DE DADOS. 


#MINUTAGEM 19:52
SELECT 
	DISTINCT(codigo) #COMANDO QUE SERVE PARA FILTRAR RESULTADOS DIFERENTES E SEM REPETIR A MESMA INFORMAÇÃO, DE FORMA DISTINTA.
FROM
	turma;

#MINUTAGEM 23:46
#LISTAR APENAS ALUNOS COM DATA DE NASCIMENTO VAZIO:
SELECT
	a.*
FROM
	aluno a
WHERE
	a.nascimento IS NULL;#VAI LISTAR TODOS OS CAMPOS VAZIOS DO CAMPO 'NASCIMENTO'
	
	#EXEMPLO: PARA LISTAR TODOS OS ALUNOS QUE ESTÃO COM OS CAMPOS PREENCHIDOS
	#a.nascimento IS NOT NULL;


#MINUTAGEM 31:18
#OPERAÇÕES SOBRE CONJUNTOS DE RESULTADOS

# *COUNT = QUANTAS LINHAS EXISTEM NO RESULTADO 
# *SUM = SOMA EX:SUM(idade)
# *AUG = MÉDIA EX:AUG(idade)
# *MAX = MÁXIMO EX:MAX(idade)
# *MIN = MINUMO EX: MIN(idade)

#QUAL A MAIOR NOTA DE TODOS OS ALUNOS
SELECT
	MAX(a.nota)
FROM
	avaliacao av;

#FILTRANDO PELO CÓDIGO NÚMERO 2
SELECT
	MAX(a.nota)
FROM
	avaliacao av
WHERE 
	av.numero_prova = 2;


#QUAL A MÉDIA DAS NOTAS DA PROVA 1?
SELECT
	MAX(av.nota)
FROM
	avaliacao av
WHERE
	av.numero_prova = 1;


#QUAL A MENOR NOTA DA PROVA 1?
SELECT
	MIN(av.nota)
FROM
	avaliacao av
WHERE
	av.numero_prova = 1;


#SUB-CONSULTAS
#QUAL O NOME DO ALUNO QUE TIROU A MENOR NOTA DA PROVA 1?
#OBS: SABENDO QUAL AMENOR NOTA PELO COMANDO ACIMA, ELE VAI ENTRAR COMO UMA SUB CONSULTA NO MODELO ABAIXO:

SELECT 
	*
FROM
	avaliacoes av2
WHERE
	av2.codigo_aluno = a.codigo
	AND av2.nota = (SELECT
				MIN(av.nota)
		    FROM
				avaliacao av
		    WHERE
				av.numero_prova = 1);

#MINUTAGEM 53:40
#QUAL A MAIOR E A MENOR NOTA DA PROVA 2 TURMA 1?
#TERÁ UMA JUNÇÃO DE 3 TABELAS

SELECT
	*
FROM
	avaliacao av, aluno a, turma t #CONSULTANDO 3 TABELAS
WHERE	
	av.numero_prova = 2 #CONDIÇÃO DE BUSCA
	AND t.codigo = 1 #CONDIÇÃO DE BUSCA
	AND av.codigo_aluno = a.codigo #RELACIOANDO AS TABELAS
	AND t.codigo_aluno = a.codigo; #RELACIONANDO AS TABELAS


#DEPOIS DE REALIZADO A BUSCA DOS DADOS, BASTA SUBSTITUIR * PELOS OPERADORES, COMO O EXEMPLO ABAIXO:

SELECT
	MAX(av.nota), MIN(av.nota) #PEGA A MAIOR E MENOR NOTA
FROM
	avaliacao av, aluno a, turma t #CONSULTANDO 3 TABELAS
WHERE	
	av.numero_prova = 2 #CONDIÇÃO DE BUSCA
	AND t.codigo = 1 #CONDIÇÃO DE BUSCA
	AND av.codigo_aluno = a.codigo #RELACIOANDO AS TABELAS
	AND t.codigo_aluno = a.codigo; #RELACIONANDO AS TABELAS



ATENÇÃO!!!
APROFUNDAR ESTUDOS SOBRE ARRAYLIST (VAI TRABALHAR DIARIAMENTE COM ELE)!!!

#OPERADOR DE TOTAL DE REGISTROS?
#QUANTOS ALUNOS ESTÃO CADASTRADOS NO BANCO?

SELECT
	COUNT(*) AS Total #SERVE COM UM CONTADOR!
FROM
	aluno;


#CAMPO DATA DE NASCIMENTO VAZIO
SELECT
	COUNT(*) AS Total #SERVE COM UM CONTADOR!
FROM
	aluno
WHERE
	a.nascimento IS NULL;


#CONTAGEM SEM ELEMENTOS REPETIDOS
#QUANTAS TURMAS DIFERENTES EXISTEM NO BANCO?

SELECT
	COUNT(DISTINCT (t.codigo))
FROM
	turma t;
































